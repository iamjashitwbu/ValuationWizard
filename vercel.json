{
  "buildCommand": "pnpm install && pnpm --filter dcf-valuation run build",
  "installCommand": "echo 'install handled in buildCommand'",
  "outputDirectory": "dcf-valuation/dist/public",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
