{
  "buildCommand": "pnpm install && pnpm --filter @workspace/dcf-valuation run build",
  "installCommand": "echo 'install handled in buildCommand'",
  "outputDirectory": "artifacts/dcf-valuation/dist/public",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
